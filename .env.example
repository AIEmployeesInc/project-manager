# Copy to ".env" only if you want to test against a real Postgres locally.
# Leave it unset for normal local development — the app uses an embedded
# Postgres (./pgdata) automatically when DATABASE_URL is not set.
#
# On Render, set this in the dashboard to your Neon connection string.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
